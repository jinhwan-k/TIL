인스턴스 메서드 
  -인스턴스 생성 후 '참조변수 메서드이름()'으로 호출
  -인스턴스 멤버(iv,im)와 관련된 작업을 하는 메서드   .iv(인스턴스 변수)사용
  -메서드 내에서 인스턴스 변수(iv) 사용가능
  -인스턴스 변수를 클래스 전체에서 사용가능
  
  
static 메서드(클래스메서드)
   -객체생성없이 '클래스이름.메서드이름()'으로 호출          (클래스이름 → math,random)
   -인스턴스 멤버(iv,im)와 관련없는 작업을 하는 메서드 . lv(지역변수)사용
   -메서드 내에서 인스턴스 변수(iv) 사용불가
   
   
   
   
  인스턴스메서드: 참조변수.메서드이름(); 으로 호출
  
  클래스메서드: 클래스이름.메서드이름(); 으로 호출
  
  
 stati메서드를 사용할때 : iv를 사용하지않을때   
 
 두 메서드의 큰차이 : iv사용여부

객체 = iv묶음.

iv = 변수묶음    메서드 = 명령문 집합
